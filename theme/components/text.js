import { colors } from "../foundations/colors";

export const textStyles = {
  baseStyle: {
    margin: 0,
    padding: 0,
    fontSize: ".875em",
    fontWeight: 400,
    color: colors.system["dark-grey"]
  }
};
